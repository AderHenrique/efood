import styled from 'styled-components'
import { cores } from '../../styles'

export const NoteContainer = styled.div`
  color: ${cores.rosa};
  font-size: 18px;
  font-weight: 700;
  display: flex;
  gap: 8px;
  max-width: 55px;
  max-height: 21px;
`
